import 'package:flutter/material.dart';
import 'package:skeletonizer/skeletonizer.dart';

PaintingEffect skeletonEffect = PulseEffect(
  from: Colors.white.withValues(alpha: 0.1),
  to: Colors.white.withValues(alpha: 0.8),
);
